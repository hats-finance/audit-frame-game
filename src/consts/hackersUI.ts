const BIG_UI = true;
const HACKERS_PER_ROW = BIG_UI ? 2 : 5;
export const HACKER_WIDTH = (900 * 0.9) / HACKERS_PER_ROW;
export const HACKER_HEIGHT = BIG_UI ? 310 : 190;
export const HACKER_MARGIN = (900 * 0.1) / 2 / HACKERS_PER_ROW;
